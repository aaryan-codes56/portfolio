# Portfolio v2 | Aaryan Krishna

A premium, minimalist portfolio built with React, focusing on clean code, smooth animations, and a high-performance user experience.

![Portfolio Preview](public/hero-image.png)

## ✨ Features

- **Premium UI/UX**: Custom glassmorphism design, radiant glows, and a consistent "black & white + accent" theme.
- **Advanced Animations**: Powered by `framer-motion` for stagger interactions, scroll-linked animations, and magnetic hover effects.
- **Scroll Spy Navbar**: Intelligent navigation that tracks your position and highlights the active section with a sliding pill animation.
- **Dynamic Projects Showcase**: "Problem-Solution" oriented project cards with distinct "Code" vs "Live" call-to-actions.
- **Responsive Design**: Fully optimized for mobile, tablet, and desktop with adaptive layouts (e.g., bottom-bar mobile nav).

## 🛠 Tech Stack

- **Core**: React 18, Vite
- **Styling**: Vanilla CSS (Variables, Flex/Grid, Glassmorphism)
- **Animation**: Framer Motion
- **Type Checking**: PropTypes

## 🚀 Getting Started

1.  **Clone the repository**
    ```bash
    git clone https://github.com/aaryan-codes56/portfolio.git
    cd portfolio
    ```

2.  **Install Dependencies**
    ```bash
    npm install
    ```

3.  **Start Development Server**
    ```bash
    npm run dev
    ```

4.  **Build for Production**
    ```bash
    npm run build
    ```

## 🎨 Customization

- **Colors**: Edit `src/styles/variables.css` to change the `--accent` hue.
- **Content**: Update data objects in `src/components/` (e.g., `projects` in `Work.jsx`) to add your own info.

---

&copy; 2025 Aaryan Krishna. Open for opportunities.
